# ==========================================
# Root environment file (shared defaults).
# Each app also has its own .env.example.
# ==========================================
NODE_ENV=development
TZ=UTC
API_PORT=4000
WEB_PORT=5173
